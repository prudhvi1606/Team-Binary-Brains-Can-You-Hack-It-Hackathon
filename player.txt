
class Player {
  constructor(game) {
    this.x = 50;   // starting position (left side)
    this.y = game.height / 2; // middle of screen
    this.width = 40;
    this.height = 40;
    this.health = 5;   // ship health
    this.ammo = 20;    // bullets available
    this.projectiles = []; // bullets the ship fires
  }
  
  update(deltaTime) {
    // Moves ship up/down
    this.y += this.speedY;

    // Recharge ammo automatically over time
  }

  draw(ctx) {
    // Draw ship as cyan box
  }

  shoot() {
    // Fires a projectile (bullet) if ammo > 0
  }
}
